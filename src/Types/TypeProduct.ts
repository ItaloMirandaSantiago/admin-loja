export type TypeProductOptional = {
    title?: string,
    description?: string,
    unit?: number,
    price?: string,
    id?: number,
    discount?: string,
    productionprice?: string
}

export type TypeProduct = {
    title: string,
    description: string,
    unit: number,
    price: string,
    id: number,
    discount: string,
    productionprice: string
}