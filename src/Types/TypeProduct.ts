export type TypeProductOptional = {
    title?: string,
    description?: string,
    unit?: number,
    price?: string,
    id?: number,
    discount?: string
}

export type TypeProduct = {
    title: string,
    description: string,
    unit: number,
    price: string,
    id: number,
    discount: string
}